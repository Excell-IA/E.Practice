# data/

Dataset di seed per la demo V0.

- `seed.json` — dataset completo caricato al boot del backend in V0 (clienti, pratiche, fasi, eventi, allegati, utenti studio, categorie, template fasi, etichette, activity log iniziale).

Scaffold reale in **F4** (Repository in-memory + seed — PR046 in poi).
