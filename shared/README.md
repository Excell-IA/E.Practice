# shared/

Codice condiviso fra backend e frontend (tipicamente: tipi TS generati da Pydantic, costanti, schemi).

V0: **vuoto**. Riservato per evoluzione.

V2: ospiterà `shared/ai/` (LLM Wrapper Standard E.Work) e `shared/utils/` (decorator `@timed`, helper di logging).
