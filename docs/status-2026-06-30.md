# Status E.Practice - 2026-06-30

## Contesto

Ripresa operativa di PR155 dopo la decisione piattaforma: le tabelle di modulo
vivono nel repo del modulo, non nel core E.Work. Il branch E.Work con DDL nel
core resta archivio tecnico (`archive/epractice-pr155-ework-db-ddl-in-core`) e
non deve essere mergiato.

Da oggi il lavoro PR155 prosegue direttamente su `main`, perche' non ci sono
altri agenti attivi sul repo E.Practice.

## Cosa e' stato fatto

- Portati su `main` i tre commit gia' presenti su
  `feature/epractice-pr155-sql-adapter`:
  - `c736e6c` - adapter SQL opzionale per storage E.Work;
  - `beb2c0b` - loader seed SQL;
  - `5fbe26e` - report decisione architetturale.
- Aggiunta infrastruttura Alembic nel repo E.Practice:
  - `backend/alembic.ini`;
  - `backend/alembic/env.py`;
  - `backend/alembic/script.py.mako`;
  - `backend/alembic/versions/practice_0001_create_practice_tables.py`.
- La migration usa:
  - revision `practice_0001`;
  - branch label `practice`;
  - tabella versione dedicata `public.alembic_version_practice`;
  - schema tenant `tenant_excellia`;
  - tabelle `practice_*`.
- Applicata la migration sul DB condiviso locale E.Work.
- Rilanciato il loader seed SQL.
- Corretto un bug runtime dello storage SQL:
  `practice_activity_log.metadata` e' JSONB e, con query testuale SQLAlchemy +
  asyncpg, va serializzato prima dell'insert.

## Verifiche

DB locale:

- `public.alembic_version_practice = practice_0001`;
- `tenant_excellia.practice_practices` presente;
- loader seed SQL OK;
- dry-run loader OK.

Comandi passati:

- `.venv/Scripts/alembic.exe -c alembic.ini heads --verbose`;
- `.venv/Scripts/alembic.exe -c alembic.ini upgrade head`;
- `.venv/Scripts/python.exe scripts/load_seed_to_sql.py --dry-run`;
- `.venv/Scripts/python.exe scripts/load_seed_to_sql.py`;
- `STORAGE_MODE=ework .venv/Scripts/python.exe scripts/demo_smoke.py`;
- `.venv/Scripts/python.exe -m ruff check app scripts/load_seed_to_sql.py alembic`;
- `.venv/Scripts/python.exe -m ruff format --check app scripts/load_seed_to_sql.py alembic`;
- `.venv/Scripts/python.exe -m mypy app scripts/load_seed_to_sql.py`;
- `.venv/Scripts/python.exe -m pytest tests`.

Risultato:

- smoke demo flow: ALL OK;
- ruff: verde;
- mypy: verde;
- pytest: 18 passed.

Nota: lo smoke con storage SQL crea una pratica runtime (`PR-2026-009`), quindi
il DB locale puo' mostrare 9 pratiche invece delle 8 del seed dopo il test.
Questo e' atteso e non viene cancellato automaticamente.

## Cosa resta aperto

PR155 ora ha il pezzo centrale DB/migration validato localmente. Restano da
decidere/chiudere:

1. push su `origin/main`;
2. aggiornamento Airtable PR155 a Done o, se si preferisce, separazione del
   prossimo lavoro in task dedicati;
3. smoke E.Practice <-> E.Contacts con E.Contacts realmente acceso;
4. montaggio dello storico E.Practice dentro E.Work Contacts;
5. passata UI Design System v2 / sostituzione della shell finta.

## Prossimo passo consigliato

Chiudere PR155 come migrazione DB module-owned, poi aprire il task successivo
per l'aggancio runtime E.Contacts/E.Work:

- `ECONTACTS_BASE_URL` reale;
- soggetto operativo azienda/persona;
- `TimelineWidget` o equivalente montato dentro E.Work Contacts;
- smoke con creazione evento su `target_type` + `target_id`.
