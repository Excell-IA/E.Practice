# Status E.Practice — 15/16 maggio 2026

Status snapshot per ripresa lavoro post sessione lunga.
Branch corrente: `fix/demo-bugs`.
Demo target: **Studio Leali, lunedì 18/05 mattina**.

---

## Sessione

- **Inizio**: venerdì 15/05 verso le 14:20
- **Fine**: sabato 16/05 alle ~01:15
- **Commit prodotti**: 31 (di cui 1 round 4 di Codex, gli altri Claude)
- **File toccati**: frontend completo (`tree/`, `practice/`, `shell/`, `ui/`, `app/`, `lib/`), backend (`routers/`, `models/`, `services/`)
- **Test usabilità reale**: Pamela ha provato il prodotto nel pomeriggio del 15/05 → da lì sono nate ~10 categorie di feedback UX che hanno guidato il resto della sessione

## Bug raccolti e chiusi (numerati progressivi)

Sono stati catalogati ~49 bug + 5 feature minori. Tutti chiusi.

### Frontend UX — drawer fase, evento, albero
- Drawer evento: feedback "Salvato ✓", refresh albero post-edit
- Drawer fase: stato come pillole 5-state cliccabili (sostituiscono dropdown), rimossi bottoni Completa/Salta duplicati, footer "Le modifiche sono salvate automaticamente / Chiudi"
- Drawer fase: "Note salvate in locale" → "Note di questa fase", "Aggiungi nota" → "Salva", feedback "Aggiunta ✓"
- Drawer fase: rimossi pulsanti "Note operative" e "Allegati" (confondevano, chiudevano il drawer)
- Nodo evento sull'albero: chip hover con tipo (Email/Telefonata/Avviso), tooltip SVG arricchito, truncate label a 16 char
- Nodo fase sull'albero: chip hover "Fase N di M", numero d'ordine visibile anche su pending, cerchio principale r=28 uniforme tra status
- Albero: container 1280→1536→3200px (più respiro orizzontale); fasi non più forzate con `Math.max(dateX, prev+175)`, ora rispettano davvero la cronologia
- Albero: titoli fasi su 2 righe max 14 char con "…", così fasi a 2-3 giorni non si sovrappongono più
- Albero: "OGGI" letto dal clock di sistema (era hardcoded `new Date("2026-03-16")`)
- Albero: timeline range = min/max di apertura/scadenza/fasi/eventi/oggi + 7g padding (eventi prima della startDate non più clampati a sinistra)
- Albero: bottone "Avviso" rimosso dalla toolbar (mappava su tipo `warning` ed era duplicato di "Nota"). Composer "Nota" ora salva davvero una nota tramite `add_note`
- Albero: eventi raggruppati per (data, tipo, fase) → un nodo con counter, click apre drawer storico ordinato
- Albero: note libere visualizzate come puntini electric sul tronco, posizionate per `occurredAt`
- Albero: toolbar consolidata ("Aggiungi evento" + data + 3 bottoni tipo), composer expanded solo al click di tipo, badge tipo grande e centrato, data non più ripetuta sotto il badge
- Hydration mismatch SVG `x1` su Next.js dev: stabilizzato `todayDate` iniziale a `2026-01-01T00:00:00Z`, swap a `new Date()` dopo mount

### Frontend UX — wizard nuova pratica
- Da 3 step a 1 unico → poi da 1 unico a 2 step con indicator + lock (cliente+dati prima, fasi+allegati dopo)
- Cliente: filtro con label, count risultati live, dropdown alfabetico, "+ Nuovo cliente" allineato nella stessa riga
- "Nuovo cliente" sheet: ora intercetta errore + banner danger inline (era un Next.js overlay sgradevole); detect P.IVA (11 cifre) vs CF (16 alfanum); bottone "Salva cliente" (era "Crea cliente e apri pratica")
- Dati pratica: label esplicite per ogni input (Tipologia / Responsabile / Apertura / Scadenza richiesta / Priorità), niente più date "nude"
- Scadenza richiesta: cambio → fasi redistribuite proporzionalmente, l'ultima snap-a alla scadenza
- Loop bugfix: `useEffect` del template aveva `scadenza` in deps → ogni redistribuzione veniva sovrascritta dal template ricaricato. Tolta `scadenza` dalle deps
- Sidebar destra: deriva dalle fasi ridistribuite, non più dal preview template originale
- Editor fasi: una data inizio per fase, durata in giorni automatica (next.start - this.start o scadenza per ultima), assegnatario per fase, riordino automatico per data. Niente più colonne `Inizio + Durata + Fine`
- Backend `PhaseOverride.assignee_id` aggiunto, service usa quello (fallback `responsible_id`)

### Frontend UX — liste e tab
- Sidebar: voce "Home" prima entry (era Dashboard), "Dashboard" rimosso, "Nuovo documento" (era "Carica documento"), voce "Tipologie pratica", voce "Utenti studio", voce "Esci" in fondo
- `/` ora redirect-only via React-only landing (logo Excellia + CTA "Entra") — il redirect Next.js iniziale di Codex aveva tolto la landing
- `/pratiche`: titolo "Elenco pratiche", eyebrow "Home", colonna Documenti con icona + count e modal lista allegati, colonna Azioni con cestino + DELETE pratica cascading
- `/clienti`: titolo "Rubrica clienti" (era "Clienti Studio Leali"), colonne ordinabili, "+" a sinistra del nome cliente per aprire wizard nuova pratica precompilato, "Nuovo cliente" sheet con stesso fix banner errore + detect P.IVA/CF
- `/utenti`: nuova pagina con ruoli editabili (Admin/Operatore/Centralino/Esterno) e toggle attivo/sospeso, backend `PATCH /api/users/{id}`
- `/tipologie`: nuova pagina con 5 categorie, ognuna mostra le fasi del template (durata)
- `/tipologie/[categoryId]`: editor del template (aggiungi/rinomina/sposta su-giù/elimina/duration), `PUT /api/templates/{category_id}` atomic replace
- Tab Timeline: badge tipo, ordinamento OGGI/passato/FUTURO con card differenziate (Codex round 4)
- Tab Note: edit nota + data editabile + delete (Codex round 2). Note non più legate a fase: phaseId opzionale ovunque, header "Nota generale della pratica"
- Tab Allegati: bottone "Scarica" ora funziona (decode base64 client-side da `storage_key`), `overflow-y-visible` per togliere scrollbar parassita
- Calendario nero su tema scuro: filter `invert(0.85)` su `::-webkit-calendar-picker-indicator`

### Help drawer "?"
- Componente `components/ui/help-button.tsx` riutilizzabile (icona ? + Sheet laterale destro con title + subtitle + children)
- Applicato a: PracticeTree toolbar, PracticesListClient header, ClientsListClient header, NewPracticeWizard header, /tipologie, /utenti, /tipologie/[categoryId]
- Sparito il vecchio `TreeHelpBox` sotto l'albero (occupava spazio fisso, ora è on-demand)

### Backend
- `Note.occurred_at` opzionale; `NoteCreate` lo accetta; `PUT /api/notes/{id}` accetta body/content + occurred_at; `DELETE /api/notes/{id}` con activity log
- `Attachment` endpoint già esistenti; upload + attach + delete; `listAttachments(?practice_id=...)`
- `PATCH /api/users/{id}` per ruolo/stato
- `DELETE /api/practices/{id}` con cascade su phases/events/notes/reminders/attachments/labels
- `PhaseOverride.assignee_id` opzionale (UUID); `create_with_template` lo usa
- `PUT /api/templates/{category_id}` atomic replace delle fasi template della categoria, activity log con scope=template
- Smoke test `backend/scripts/demo_smoke.py` sempre verde lungo tutta la sessione

## Cosa funziona end-to-end (verificato)

- Landing → Entra → Elenco pratiche
- Wizard nuova pratica step 1 (cliente + dati) → step 2 (fasi + allegati) → Crea
- Click su pratica → dettaglio con tab Info/Albero/Timeline/Allegati/Note/Anagrafica
- Albero: nodi fase, eventi (call/mail con raggruppamento), note come puntini; click sui nodi apre drawer; click sul tronco aggiorna data composer
- Drawer fase: stato pillole, assegnatario, data, note inline (add/edit/delete)
- Drawer evento: title/description/data/autore con feedback Salvato
- Tab Note: list + add con data + edit + delete
- Tab Allegati: upload + lista + download + delete
- Rubrica clienti: ricerca, ordinamento, "+ Nuovo cliente", "+" per nuova pratica
- Lista pratiche: filtri, ricerca, modal allegati per riga, eliminazione con confirm
- /utenti: switch ruolo e stato (solo admin)
- /tipologie + /tipologie/[id]: visualizza categorie e modifica template di fasi

## Stato componenti

- **Server locali**: backend uvicorn su `127.0.0.1:8000`, frontend Next.js su `127.0.0.1:3000`. Entrambi up al momento di scrivere
- **DB**: in-memory (V0), reset al restart del backend (caveat documentato)
- **Test**: `tsc --noEmit` verde, `next lint` verde, `ruff check/format` verde, `mypy app` verde, smoke test verde

## Problemi noti / open

1. **Errore "1 error" toast** che lampeggia ogni tanto nell'overlay Next.js dev. L'utente non è riuscito a leggerlo, può essere il residuo di un hydration mismatch precedente (fix di `c859c9b`) oppure altro. **Da indagare** con DevTools aperti.
2. **2 warning "form field should have id/name"** in DevTools: cosmetico, non blocker. Da aggiungere `name` agli input principali quando c'è tempo (V1).
3. **Sovrapposizione nodi fase su date molto vicine**: con SVG 3200px è migliorata ma con 2 fasi a 1 giorno di distanza i cerchi possono toccarsi. Risolvibile con alternanza Y, ma per V0 accettato.
4. **Eventi di tipo `warning` legacy nel seed**: il bottone "Avviso" è stato rimosso, ma gli eventi warning del seed esistono ancora e vengono renderizzati come `warning`. Non se ne possono creare di nuovi. OK per demo.
5. **TabNotes con phase_id**: le note nuove sono libere, quelle vecchie del seed hanno phaseId. La sezione "Note di questa fase" nel drawer mostra solo quelle con phaseId coincidente. OK per demo, semantica chiarita nella copy.
6. **`Nota` come tipo evento backend**: la nota viene salvata via `add_note` API quindi è una vera Note backend, non un PracticeEvent. Pulito ora.

## TODO per domani (sabato 16/05) o lunedì 18/05

### Bloccanti per la demo
1. **Tu**: crea account Vercel + Render con login GitHub (~15 min). Prompt step-by-step già fornito in chat il 15/05 pomeriggio
2. **Io**: deploy backend su Render + frontend su Vercel seguendo `docs/deploy.md`
3. **Io**: smoke test sugli URL pubblici post-deploy
4. **Io**: bozza mail Studio Leali (link + caveat cold start 20s, dati in-memory resettano su restart, "MVP regalo")
5. **Tu**: rivedi la mail e invia (lunedì 18/05 mattina)

### Pre-deploy: merge in main
- `fix/demo-bugs` ha ~80 commit dall'inizio del giro fix. Merge in `main` prima del deploy così Render/Vercel puntano alla branch ufficiale

### Nice-to-have post-demo (V0.1)
- Drag-drop fasi sull'albero (l'utente l'aveva segnato come wow V1)
- Agenda personale (filtra fasi per assegnatario)
- Note: raggruppamento multi-nota stesso giorno (oggi si sovrappongono)
- Notifiche email reali su Reminder (oggi solo backend in-memory)

## File chiave

- `frontend/components/tree/PracticeTree.tsx` — vista albero completa
- `frontend/components/tree/NodeDrawer.tsx` — drawer fase/evento
- `frontend/components/tree/EventNode.tsx`, `EventGroupNode.tsx`, `PhaseNode.tsx`
- `frontend/components/practice/NewPracticeWizard.tsx` — wizard 2-step
- `frontend/components/practice/PracticesListClient.tsx` — lista + cestino + modal allegati
- `frontend/components/practice/ClientsListClient.tsx` — rubrica
- `frontend/components/practice/TabNotes.tsx`, `TabTimeline.tsx`, `TabAllegati.tsx`
- `frontend/components/shell/EWorkShell.tsx` — sidebar + header
- `frontend/components/ui/help-button.tsx` — drawer guida riusabile
- `frontend/app/tipologie/page.tsx` + `frontend/app/tipologie/[categoryId]/page.tsx`
- `backend/app/routers/templates.py` — PUT atomic replace
- `backend/app/routers/practices.py` — DELETE cascade
- `backend/app/routers/notes.py` — PUT/DELETE + occurred_at
- `backend/app/services/practice_service.py` — assignee_id override
- `data/seed.json` — 8 pratiche + 10 clienti + 4 utenti + 5 categorie

## Commit list (31 della sessione 15-16/05)

```
05-15 15:06  ddeec00  Bugfix: event drawer save feedback and tree date sync
05-15 15:12  0553a89  Bugfix: read OGGI from system clock instead of hardcoded 2026-03-16
05-15 15:13  e15e369  Bugfix: timeline filters and editable notes
05-15 15:17  f394fce  Bugfix: clearer phase vs event nodes on tree
05-15 15:21  44e1464  Bugfix: visible date picker icon on dark themes + drop stale demo notes
05-15 15:21  c03648c  Bugfix: timeline range covers events and phases outside practice window
05-15 15:42  9c992d5  Bugfix: tree event label truncation + new note date + toolbar nomenclature
05-15 16:21  4da7017  Bugfix UX: wider practice detail layout + tree help box
05-15 16:25  c992f9a  Bugfix UX round 4: home + wizard unico + utenti studio + timeline rework
05-15 16:29  d9272a9  Bugfix UX: group same-day same-type events with counter + history drawer
05-15 16:33  b8b7489  Bugfix UX: surface attachments count on practice list with detail sheet
05-15 16:42  dc9147a  Chore: silence TS 7.0 deprecation warnings for target=ES5 and baseUrl
05-15 16:56  c56f34a  Bugfix UX: clearer phase drawer (autosave feedback, drop redundant bits)
05-15 16:59  a282d03  Bugfix UX: restore landing page, add Esci, fix attachment download
05-15 17:45  2e2de2b  Bugfix UX: 2-step wizard + practice types page + Nuovo cliente alignment
05-15 17:48  ae9fa10  Bugfix UX: clients page title aligned with sidebar label
05-15 22:39  a2158eb  Bugfix UX: client creation error handling + clearer client filter
05-15 22:47  6ab9acb  Bugfix UX: phases editor rewritten (single date + duration + per-phase assignee + auto reorder)
05-15 22:49  d906a48  Bugfix UX: phase status as pill row, drop redundant Completa/Salta buttons
05-15 22:55  fdeb143  Bugfix UX: tree toolbar consolidated + clearer help box
05-15 23:09  03b58e8  Bugfix UX: delete practice + clearer Nuovo cliente sheet in rubrica
05-15 23:16  14f34cb  Bugfix UX: wizard date labels + scadenza redistributes phases + note feedback
05-15 23:20  6960db4  Bugfix UX: tree respects date proportions + clearer composer + Salva label
05-15 23:40  0bd62b6  Bugfix UX: notes on tree, drop Avviso, truncate phase titles
05-15 23:45  c859c9b  Bugfix: avoid SSR hydration mismatch on tree by stabilising todayDate
05-16 00:37  cf609da  Bugfix UX: widen tree SVG canvas to give phase labels breathing room
05-16 00:41  1f8e5c7  Bugfix UX: preview sidebar follows the redistributed phases
05-16 00:50  0ab978a  Bugfix: drop Fine column in phases editor + stop scadenza redistribution loop
05-16 00:57  66f9ae2  Bugfix: phases use one date, notes are practice-wide, composer badge centered
05-16 01:08  d5ae016  Feat: reusable HelpButton drawer + applied to main screens
05-16 01:12  d09fdd8  Feat: editable template per category — backend PUT + frontend editor
```
